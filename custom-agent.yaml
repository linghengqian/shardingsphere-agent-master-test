plugins:
  tracing:
    OpenTelemetry:
      props:
        otel.service.name: "shardingsphere-agent-master-test"
        otel.traces.sampler: "always_on"
        otel.traces.exporter: "otlp"
        otel.exporter.otlp.traces.endpoint: "http://jaeger:4317"
